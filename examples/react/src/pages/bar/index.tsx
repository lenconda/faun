import React from 'react';
import styles from './index.less';

const BarPage: React.FC = () => {
  return (
    <h1 className={styles['bar-title']}>Bar Route in React.js App</h1>
  );
};

export default BarPage;
